<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Team7Final.Views.TaskListPage" BackgroundColor="#E4E4E4"
         Title="To-do">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="New Item" Clicked="OnItemAdded">
            <ToolbarItem.IconImageSource>
                <OnPlatform x:TypeArguments="ImageSource">
                    <On Platform="Android, UWP"
                    Value="plus.png" />
                </OnPlatform>
            </ToolbarItem.IconImageSource>
        </ToolbarItem>
    </ContentPage.ToolbarItems>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackLayout Grid.Row="0" Orientation="Horizontal" Margin="20,10">
            <Label Text="Incomplete Only" VerticalOptions="Center" />
            <Switch x:Name="toggleSwitch" Toggled="OnToggleSwitchToggled" />
        </StackLayout>

        <ListView x:Name="listView" Grid.Row="1" Margin="20" ItemSelected="OnListItemSelected">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Margin="30,0,0,0"
                                 Orientation="Horizontal"
                                 HorizontalOptions="FillAndExpand">
                            <Label Text="{Binding Name}"
                               HorizontalOptions="StartAndExpand"
                               
                               VerticalTextAlignment="Center"
                               
                               FontSize="25" />
                            <CheckBox HorizontalOptions="End"
                                      IsChecked="{Binding Done}"
                                      CheckedChanged="CheckBox_CheckedChanged"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</ContentPage>